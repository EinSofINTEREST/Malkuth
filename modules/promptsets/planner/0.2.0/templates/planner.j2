Question: {{ query }}

Produce a research plan at depth {{ depth }}.
Return the plan as a numbered list, one step per line.

Respond with a JSON object containing exactly these keys: plan, needs_research
