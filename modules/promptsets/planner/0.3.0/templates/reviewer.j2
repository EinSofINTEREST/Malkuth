Draft under review:
{{ draft }}

Decide whether this draft is ready. If it is not, say what must change —
each point separately, so the writer can address them one by one.

Respond with a JSON object containing exactly these keys: approved, notes
