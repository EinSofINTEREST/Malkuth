Feeds to check:
{{ feeds }}

Report any items you have not seen before, as a list of item ids.
If there are none, return an empty list — the graph routes on whether it is
empty, so any placeholder there would be read as "there is work".

Respond with a JSON object containing exactly these keys: new_items
