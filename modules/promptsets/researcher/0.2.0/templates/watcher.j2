Feeds to check:
{{ feeds }}

Report any items you have not seen before.
If there are none, set new_items to an empty string — the graph routes on
whether it is empty, so any prose there would be read as "there is work".

Respond with a JSON object containing exactly these keys: new_items
