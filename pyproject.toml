[project]
name = "malkuth"
version = "0.1.0"
description = "Modular multi-agent orchestration framework on LangGraph"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}

dependencies = [
    "langgraph>=0.2.0",
    "langchain-core>=0.3.0",
    "pydantic>=2.9.0",
    "structlog>=24.4.0",
    "prometheus-client>=0.21.0",
    "tenacity>=9.0.0",
    "jinja2>=3.1.0",
    "pyyaml>=6.0.2",
    "httpx>=0.27.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "docker>=7.1.0",
    "a2a-sdk>=0.2.0",
    "mcp>=1.1.0",
    "psycopg[binary]>=3.2.0",
    "anthropic>=0.40.0",
    "langgraph-checkpoint-postgres>=3.1.2",
]

[dependency-groups]
dev = [
    "ruff>=0.8.0",
    "mypy>=1.13.0",
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "pytest-benchmark>=5.1.0",
    "types-pyyaml>=6.0.12",
    "testcontainers[postgres]>=4.8.0",
]

[project.scripts]
malkuth = "malkuth.cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/malkuth"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "ASYNC", "S", "C4", "RET", "SIM", "PTH"]

[tool.ruff.lint.isort]
known-first-party = ["malkuth", "tests"]

[tool.ruff.lint.per-file-ignores]
# 테스트는 assert 사용이 본질 (S101), 픽스처 스코프상 하드코딩 값 허용 (S105~S107)
"tests/**" = ["S101", "S105", "S106", "S107"]

[tool.mypy]
python_version = "3.12"
packages = ["malkuth"]
mypy_path = "src"
warn_unused_configs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true

# core 는 프레임워크 계약 — strict 필수 (06-testing.md Quality Gates)
[[tool.mypy.overrides]]
module = "malkuth.core.*"
strict = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: Docker/실서버 fixture 를 사용하는 통합 테스트",
    "e2e: 전체 스택 E2E 테스트 (nightly)",
]
addopts = [
    "--strict-markers",
    "--cov=malkuth",
    "--cov-report=term-missing",
    "--cov-fail-under=70",
]

[tool.coverage.run]
source = ["src/malkuth"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "@abstractmethod",
    "\\.\\.\\.",
]
